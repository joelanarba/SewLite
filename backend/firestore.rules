rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny read/write access to everyone by default
    // The backend uses Firebase Admin SDK which bypasses these rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
